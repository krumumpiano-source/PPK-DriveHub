# PPK DriveHub - Agent Rules

## Git Rules
- ทุกครั้งที่ทำงาน/แก้ไขโค้ดเสร็จแล้ว ให้รันคำสั่ง `git add .`, `git commit -m "..."`, และ `git push` เพื่ออัปเดตโค้ดไปยัง repository เสมอ (ตามคำสั่ง: "push dev commit ทุกครั้งที่มีการแก้ไข")
